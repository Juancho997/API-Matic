import swaggerJsDoc from 'swagger-jsdoc';

const swaggerOptions = {
    definition: {
        openapi: "3.0.0",
        info: {
            title: "{{proyect_name}}",
            version: "1.0.0",
            description: "API REST generated thanks to API-Matic, by Juancho997 > https://github.com/Juancho997 :D"
        },
        servers: [
            {
                url: `${process.env.URL}`
            }
        ]
    },
    apis: ["./src/routes/*.routes.js"]
};

const swaggerSpecs = swaggerJsDoc(swaggerOptions);

export default swaggerSpecs;